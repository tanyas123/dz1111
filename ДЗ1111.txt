

2. Удалите все файлы php с репозитория и создайте 3 файла: 1.php, 2.php и 3.php. 
В каждом файле добавьте базовый html шаблон и меню для перехода по этим файлам. 
Создайте в каждом файле переменную $page и сохраните номер страницы. Внесите 
изменение в меню файла 1.php изменения так, чтобы пункт меню для перехода на самого 
себя был выделен жирным.

3. Добавьте для каждого пункта меню файла 1.php условия которые позволят скрипту 
при изменении вручную номера страницы выделять жирным соответствующий пункт меню. 
Скопируйте получившиеся меню без изменения в остальные файлы и убедитесь, что при 
переходе в любой файл подсвечивается нужный пункт меню.


7. Ответьте на вопросы:

a. Какое значение получится в каждом случае и почему:

$a = 5;

$b = '05';

var_dump($a == $b); \\bool(true) - после преобразования типа $b из строки в число значение двух переменных одинаковое

var_dump((float)'012345'); \\float(12345) - float преобразует число из строки

var_dump((float)123.0 === (int)123.0); \\ bool(false) - при тождественном "равно" типы переменных должны быть одинаковые

var_dump((int)0 === (int)'hello, world'); \\ bool(true) - (int)'hello, world' вернет 0

b. Для чего нужны отступы при использовании if?